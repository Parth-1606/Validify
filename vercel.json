{
  "buildCommand": "cd validify && npm install && npm run build",
  "outputDirectory": "validify/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
